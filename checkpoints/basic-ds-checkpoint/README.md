# Contact Management System

A command-line contact manager demonstrating a doubly linked list, a hash table,
and naive substring matching.

Run the application:

```powershell
python contact_manager.py
```

Run the tests:

```powershell
python -m unittest -v
```
